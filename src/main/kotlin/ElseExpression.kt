fun main() {
    /*
    * if(kondisi boolean){
    * jika true, akan dieksekusi
    * } else if(kondisi boolean 2) {
    * // jika true, akan dieksekusi
    * } else {
    * // jika semua kondisi ga terpenuhi, ini yang akan dieksekusi
    * }
    * */

    val nilai = 65

    if (nilai > 75){
        println("Good Job")
    } else if (nilai > 60){
        println("Not Bad")
    }else{
        println("Silakan Coba lagi")
    }
}